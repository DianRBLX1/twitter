{
  "name": "Translator Bot",
  "description": "Translator bot Twitter is a useful too which can translate all Tweets(Excluding RTs and Replies) into any desired language",
  "repository": "https://github.com/nishanksisodiya/translate-tweet-bot",
  "stack": "container",
  "env": {
    "CONSUMER_KEY": {
      "description": "Get this value from developer.twitter.com.",
      "required": true
    },
    "CONSUMER_SECRET": {
      "description": "Get this value from developer.twitter.com.",
      "required": true
    },
    "ACCESS_TOKEN": {
      "description": "Get this value from developer.twitter.com.",
      "required": true
    },
    "ACCESS_TOKEN_SECRET": {
      "description": "Get this value from developer.twitter.com.",
      "required": true
    },
    "STALK_ID": {
      "description": "ID for the account whos tweets u want to translate (get it from http://gettwitterid.com)",
      "required": true
    },
    "TRANSLATE_TO": {
      "description": "Language u want the tweets to be translated to get lnaguage code from here https://en.wikipedia.org/wiki/List_of_ISO_639-1_codes (USE ISO 639-1 CODES)",
      "required": true
    }
  }
}